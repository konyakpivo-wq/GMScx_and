<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom OAuth Scheme Deep Link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.gmscx.services" android:host="oauth" />
            </intent-filter>
        </activity>

        <!-- ContentProvider for com.yt.cx account sharing -->
        <provider
            android:name=".data.AccountContentProvider"
            android:authorities="com.gmscx.services.provider;com.yt.cx.accounts"
            android:exported="true" />

        <!-- BroadcastReceiver for com.yt.cx account sharing request -->
        <receiver
            android:name=".data.AccountShareReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.gmscx.services.GET_ACCOUNTS" />
                <action android:name="com.yt.cx.GET_ACCOUNTS" />
                <action android:name="com.yt.cx.REQUEST_ACCOUNTS" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
